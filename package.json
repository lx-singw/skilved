{
  "name": "skilved",
  "version": "1.0.0",
  "private": true,
  "description": "Skilved \u2014 AI-powered skills opportunity platform for South Africa",
  "workspaces": [
    "apps/*",
    "apps/agents/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "web:lint": "pnpm --filter @skilved/web lint",
    "web:typecheck": "pnpm --filter @skilved/web typecheck",
    "web:build": "pnpm --filter @skilved/types build:contracts && pnpm --filter @skilved/web build",
    "web:test": "pnpm --filter @skilved/web test:boundary",
    "contracts:typecheck": "pnpm --filter @skilved/types typecheck:contracts",
    "contracts:test": "pnpm --filter @skilved/types test:contracts",
    "contracts:inspect": "pnpm --filter @skilved/types inspect:contracts",
    "catalogue:test": "pnpm --filter @skilved/types build:contracts && firebase emulators:exec --config packages/catalogue/firebase.test.json --project demo-skilved-b03 --only firestore,auth \"pnpm --filter @skilved/catalogue test\"",
    "catalogue:operator": "pnpm --filter @skilved/types build:contracts && pnpm --filter @skilved/catalogue operator"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
